package MutliThreading_Ex;

public class Multi_Threaing_Explaining {

	/*
	 * Multithreading
	 * 
	 * Many threads running at a time
	 * 
	 * Advs
	 * 
	 * 1. Faster execution
	 * Performance will be increased
	 * Microservice / web servies : provided by different company
	 * makemy trip ask companies to give me searching api to run microservice
	 * so multitasking happend multiple client requests processed at a time
	 * as like indigo different companies give search api to give filight data there are 
	 * 
	 * 
	 * we have class file java.lang.thread
	 * 
	 * extending thread class 1 way
	 * 
	 * 2nd runnable interface
	 * 
	 * 
	 * different methods

	 * Start() -starter thread
	 * run() - executing the thread
	 * sleep() - pause for certian amounto og time
        join() - wait for a thread to finish 
        interrupt() -interrupt sleeping thread
        yield() pausing to allow other threads to execute
	 * */
}
